import { redirect } from 'next/navigation';

// Placeholder: individual transmission pages will redirect to the hub
// until the YouTube channel launches and we have real video content
export default function TransmissionPage() {
  redirect('/transmissions');
}
